apiVersion: apps/v1
kind: Deployment
metadata:
  name: grad-project
  namespace: ramyanwar
spec:
  replicas: 1
  selector:
    matchLabels:
      app: java-app
  template:
    metadata:
      labels:
        app: grad-project
    spec:
      containers:
      - name: grad-project
        image: docker.io/ramy282/grad-project:7
        ports:
        - containerPort: 8082
